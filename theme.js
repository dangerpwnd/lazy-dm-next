import { roboto } from '@theme-ui/presets';

const theme = {
    ...roboto,
    containers: {
        card: {
            p:2,
        },
        page: {
            m: 0,
            p: 1,
        }
    },
    styles: {
        ...roboto.styles
    }
}

export default theme;